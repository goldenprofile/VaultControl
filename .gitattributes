# Репозиторий хранит LF. eol=lf гасит подстановку CRLF при checkout на Windows
# (core.autocrlf=true) — из-за неё git предупреждал на каждый git add.
# text=auto оставляет бинарные файлы в покое: конверсия применяется только к тем,
# что git распознал как текст.
* text=auto eol=lf
